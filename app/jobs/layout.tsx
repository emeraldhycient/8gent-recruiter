import type React from "react"
export default function JobsLayout({ children }: { children: React.ReactNode }) {
  // Keeps routing simple; pages already wrap with KokonutUI Layout
  return children
}
